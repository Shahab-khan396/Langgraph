[project]
name = "langgraph-toturial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shahab-khan396", email = "ksab2922@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.4.0",
    "langchain-openai>=1.6.2",
    "langgraph>=1.2.11",
    "langgraph-cli[inmem]>=0.4.31",
    "python-dotenv>=1.2.3",
]

[project.scripts]
langgraph-toturial = "langgraph_toturial:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
